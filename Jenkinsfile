pipeline {
    agent any

    stages {

        stage ('Prepare'){
            steps {
                echo 'Preparing the pipeline'
            }
        }
        stage('Build') {
            steps {
                echo 'Building the pipeline'
            }
        }

        stage('deploy') {
            steps {
                echo 'Deploying the pipeline'
            }
        }

    }
}